name: Build and Deploy Containers

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      skip_deploy:
        description: Skip remote deployment (only build & push images)
        required: false
        default: "false"
        type: choice
        options:
          - "false"
          - "true"

env:
  REGISTRY: docker.io/${{ secrets.DOCKERHUB_USERNAME }}
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build-and-push:
    name: Build ${{ matrix.component }} image
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - component: backend
            context: backend
            dockerfile: backend/Dockerfile
            image: kuba-backend
          - component: public
            context: frontend/public
            dockerfile: frontend/public/Dockerfile
            image: kuba-public
          - component: display
            context: frontend/display
            dockerfile: frontend/display/Dockerfile
            image: kuba-display
          - component: admin
            context: frontend/admin
            dockerfile: frontend/admin/Dockerfile
            image: kuba-admin
          - component: audio
            context: frontend/audio
            dockerfile: frontend/audio/Dockerfile
            image: kuba-audio
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          registry: docker.io
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3


      - name: Prepare admin build args
        if: matrix.component == 'admin'
        id: admin_args
        env:
          ADMIN_USERNAME: ${{ secrets.ADMIN_PANEL_USERNAME || 'admin' }}
          ADMIN_PASSWORD: ${{ secrets.ADMIN_PANEL_PASSWORD || 'kuba-manager' }}
          ADMIN_SESSION_KEY: ${{ secrets.ADMIN_PANEL_SESSION_KEY || 'kuba-admin-session' }}
        run: |
          {
            echo "args<<EOF"
            echo "VITE_ADMIN_USERNAME=$ADMIN_USERNAME"
            echo "VITE_ADMIN_PASSWORD=$ADMIN_PASSWORD"
            echo "VITE_ADMIN_SESSION_KEY=$ADMIN_SESSION_KEY"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Build and push ${{ matrix.component }}
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          push: true
          build-args: ${{ matrix.component == 'admin' && steps.admin_args.outputs.args || '' }}
          tags: |
            ${{ env.REGISTRY }}/${{ matrix.image }}:${{ github.sha }}
            ${{ env.REGISTRY }}/${{ matrix.image }}:latest

  deploy:
    name: Update remote stack
    runs-on: ubuntu-latest
    needs: build-and-push
    if: github.event_name != 'workflow_dispatch' || inputs.skip_deploy != 'true'
    steps:
      - name: Check disk space before deploy
        uses: appleboy/ssh-action@v1.2.2
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.DEPLOY_KEY }}
          script: |
            AVAIL=$(df / --output=avail | tail -1)
            AVAIL_GB=$(( AVAIL / 1024 / 1024 ))
            echo "Available disk space: ${AVAIL_GB} GB"
            if [ "$AVAIL" -lt 2097152 ]; then
              echo "ERROR: Less than 2 GB free on disk. Run 'cleanup' via manage-stack first."
              exit 1
            fi

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.2.2
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.DEPLOY_KEY }}
          script: |
            set -eo pipefail
            cd ${{ secrets.DEPLOY_PATH }}
            git fetch origin main
            git checkout main
            # Remove root-owned dirs Docker created instead of files for bind-mounts
            DEPLOY=${{ secrets.DEPLOY_PATH }}
            docker run --rm -v "${DEPLOY}:/repo" alpine sh -c "rm -rf /repo/plausible-ce"
            git reset --hard origin/main
            docker image prune -f || true
            docker compose pull
            docker compose up -d --remove-orphans
